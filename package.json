{
  "name": "nikita",
  "version": "2.0.0",
  "engines": {},
  "scripts": {
    "start": "nodemon --exec npx babel-node server.js",
    "build": "babel src --out-dir dist --source-maps",
    "copyFiles": "cp -rn src/* dist"
  },
  "dependencies": {
    "express": "^4.13.4",
    "jade": "^1.11.0"
  },
  "devDependencies": {
    "@babel/core": "^7.21.8",
    "@babel/node": "^7.20.7",
    "@babel/preset-env": "^7.21.5",
    "nodemon": "^2.0.22"
  }
}
