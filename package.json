{
  "name": "nikita",
  "version": "2.0.0",
  "scripts": {
    "watch": "nodemon --exec babel-node ./src/server.js",
    "build": "babel src --out-dir dist --source-maps",
    "test": "mocha",
    "copyFiles": "rm -r dist/* && cp -rn src/* dist"
  },
  "dependencies": {
    "express": "^4.13.4",
    "jade": "^1.11.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@babel/core": "^7.21.8",
    "@babel/node": "^7.20.7",
    "@babel/preset-env": "^7.21.5",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22"
  }
}
